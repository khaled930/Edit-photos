##  وصف المشروع
هذا المشروع عبارة عن منصّة ويب لمعالجة الصور الرقمية، تم تطويرها باستخدام **Python + FastAPI** في الخلفية، و **HTML / CSS / JavaScript** في الواجهة.
تتيح المنصّة للمستخدم إجراء عمليات أساسية على الصور مثل رفع الصورة، تدويرها، قصّها بشكل تفاعلي، وحفظ النتيجة النهائية.

يركّز المشروع على تطبيق مفاهيم **الوسائط المتعددة ومعالجة الصور الرقمية** بشكل عملي وتفاعلي.

##  أهداف المشروع

* فهم آلية التعامل مع الصور الرقمية
* تطبيق عمليات المعالجة الأساسية (قص، تدوير)
* ربط واجهة المستخدم مع الخادم باستخدام FastAPI
* تنفيذ قص تفاعلي مشابه لمحررات الصور
* حفظ نتائج المعالجة بدون تعديل الصورة الأصلية

##  التقنيات المستخدمة

* **Backend:** Python, FastAPI
* **Frontend:** HTML, CSS, JavaScript
* **Image Processing:** Pillow (PIL)
* **Templates:** Jinja2

##  الميزات
* رفع الصور من الجهاز
* عرض الصورة داخل المتصفح
* تدوير الصورة بزوايا مختلفة (90°، 180°، 270°)
* قص الصورة بشكل تفاعلي باستخدام الماوس
* حفظ الصورة الناتجة بعد التعديل
* الحفاظ على النسخة الأصلية من الصورة

##  هيكلية المشروع

```
image_editor_project/
│
├── app/
│   ├── main.py
│   ├── routes/
│   │   └── image_routes.py
│   ├── services/
│   │   └── image_service.py
│   ├── static/
│   │   └── uploads/
│   └── templates/
│       └── index.html
│
├── static/
│   └── style.css
│
├── requirements.txt
└── README.md
```

##  طريقة التشغيل

1. تثبيت المتطلبات:

```bash
pip install -r requirements.txt
```

2.🧪 تشغيل السيرفر:

```bash
uvicorn app.main:app --reload
```

3. فتح التطبيق من المتصفح:

```
http://127.0.0.1:8000/editor
```

##  طريقة الاستخدام

1. رفع صورة من الجهاز
2. تحديد جزء من الصورة بالماوس (قص تفاعلي)
3. تدوير الصورة إذا لزم
4. تنفيذ عملية القص
5. حفظ الصورة المعدّلة على الجهاز
